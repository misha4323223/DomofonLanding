// Vike configuration for static site generation
export default {
  // Enable prerendering (SSG) for all pages
  prerender: true,
  
  // Client-side routing with wouter
  clientRouting: true,
  
  // Hydration for React
  hydrationCanBeAborted: true,
}
